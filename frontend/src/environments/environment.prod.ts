// Production environment configuration
export const environment = {
  production: true,
  apiUrl: 'http://backend:8000'  // Backend API base URL for production (Docker service name or production URL)
};
